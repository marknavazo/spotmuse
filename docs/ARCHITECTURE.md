# Arquitectura SpotMuse

- Frontend: React + Vite
- Styling: Material UI
- Auth & DB: Firebase Auth + Firestore
- Search: Spotify API (token via VITE_SPOTIFY_TOKEN)
- CI/CD: GitHub Actions (uses FIREBASE_TOKEN secret)
